/**
 * Configure pyLoad downloader.
 *
 * @example
 * const config = await configure();
 */
async function configure() {
  throw new Error('pyLoad not impleted yet');
}

module.exports = { configure };
